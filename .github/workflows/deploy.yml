name: deploy
on:
  workflow_run:
    workflows: [eslint,jest]
    branches: [main]
    types: 
      - completed
      - requested
jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: yarn install
        run: yarn install